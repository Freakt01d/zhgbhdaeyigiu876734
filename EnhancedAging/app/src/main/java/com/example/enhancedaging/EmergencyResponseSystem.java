package com.example.enhancedaging;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class EmergencyResponseSystem extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_emergency_response_system); // Use your layout here

        // If you still want to handle insets or apply padding due to system bars but without EdgeToEdge,
        // you can use WindowInsetsController or similar APIs depending on your target API level.
    }
}
